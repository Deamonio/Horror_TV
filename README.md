<div align="center">

# 📺 Horror TV

### *A Creepy, Old-Fashioned TV with a Twist* 👻

> **Turn the dial to experience a chilling atmosphere...  then a hand slams against the screen!  😱**

![C++](https://img.shields.io/badge/C++-100%25-00599C?style=flat&logo=c%2B%2B&logoColor=white)
![Arduino](https://img.shields.io/badge/Arduino-00979D?style=flat&logo=arduino&logoColor=white)
![Horror](https://img.shields.io/badge/Genre-Horror-8B0000?style=flat)

![GitHub Stars](https://img.shields.io/github/stars/Deamonio/Horror_TV?style=social)
![GitHub Forks](https://img.shields.io/github/forks/Deamonio/Horror_TV?style=social)

---

### 🎥 Demo Video

[![Horror TV Demo](https://img.youtube.com/vi/zUugwbhxXiI/maxresdefault.jpg)](https://www.youtube.com/watch?v=zUugwbhxXiI)

**▶️ 클릭하여 공포 체험 영상 보기**

*다이얼을 돌리면...   갑자기 손이 화면을 친다!* 

---

### 📸 Product Image

![Horror TV](Horror_Tv.JPG)

*완성된 Horror TV - 빈티지 TV 안에 숨겨진 공포*

</div>

---

## 📋 목차

- [프로젝트 소개](#-프로젝트-소개)
- [시스템 아키텍처](#-시스템-아키텍처)
- [주요 기능](#-주요-기능)
- [하드웨어 구성](#-하드웨어-구성)
- [회로 구성](#-회로-구성)
- [설치 방법](#-설치-방법)
- [사용 방법](#-사용-방법)
- [코드 분석](#-코드-분석)
- [커스터마이징](#-커스터마이징)

---

## 🎯 프로젝트 소개

**Horror TV**는 레트로 TV 외관에 숨겨진 **인터랙티브 공포 체험 장치**입니다.  

### 💡 컨셉

> *"오래된 TV 채널을 돌리다가...   갑자기 화면 안에서 손이 튀어나온다면?!"*

### 🌟 특징

- 👁️ **인터랙티브**:  다이얼을 돌려 채널 변경 시뮬레이션
- 🎃 **서프라이즈**:  예측 불가능한 타이밍에 손이 튀어나옴
- 🔊 **사운드**: 공포스러운 배경음과 효과음
- 💡 **조명 효과**: LED로 불안한 분위기 연출
- 🤖 **Arduino 기반**: 간단한 하드웨어로 구현

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────┐
│  사용자 인터랙션                        │
│  ┌───────────────────────────────────┐ │
│  │  1. TV에 접근 (10cm 이내)        │ │
│  │     ↓                             │ │
│  │  2. 로터리 엔코더 다이얼 회전     │ │
│  │     ↓                             │ │
│  │  3. 카운터 증가                   │ │
│  │     ↓                             │ │
│  │  4. 랜덤 타이밍 도달 시...         │ │
│  │     ↓                             │ │
│  │  5. 💥 서보 모터 작동!             │ │
│  │     ↓                             │ │
│  │  6. 😱 손이 화면을 친다!          │ │
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  Arduino Uno (메인 컨트롤러)            │
│  ┌───────────────────────────────────┐ │
│  │  초음파 센서 → 거리 감지          │ │
│  │  로터리 엔코더 → 회전 감지        │ │
│  │  서보 모터 → 손 동작 제어         │ │
│  │  DFPlayer Mini → 공포 사운드      │ │
│  │  릴레이 → LED 조명 제어           │ │
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

---

## ✨ 주요 기능

### 1. 👁️ 근접 감지 (초음파 센서)

**HC-SR04 초음파 센서**

```cpp
// 거리 측정
long duration, distance;
digitalWrite(trig, LOW);
delayMicroseconds(2);
digitalWrite(trig, HIGH);
delayMicroseconds(10);
digitalWrite(trig, LOW);

duration = pulseIn(echo, HIGH);
distance = duration * 17 / 1000; // cm 단위

if (distance <= 10) {
    // 10cm 이내 접근 시 활성화
}
```

**작동 원리:**
- 초음파 발신 → 반사파 수신
- 시간차로 거리 계산
- 10cm 이내 접근 시 시스템 활성화

---

### 2. 🎛️ 다이얼 회전 감지 (로터리 엔코더)

**KY-040 로터리 엔코더**

```cpp
int counter = 0;
int currentStateCLK;
int lastStateCLK;

currentStateCLK = digitalRead(CLK);

if (currentStateCLK != lastStateCLK && currentStateCLK == 1) {
    if (digitalRead(DT) != currentStateCLK) {
        counter++;  // 시계방향
        currentDir = "right";
    } else {
        counter--;  // 반시계방향
        currentDir = "left";
    }
}
```

**특징:**
- 시계/반시계 방향 감지
- 무한 회전 가능
- 채널 변경 시뮬레이션

---

### 3. 🎃 랜덤 서프라이즈 (서보 모터)

**SG90 서보 모터**

```cpp
long randNumber = random(50); // 랜덤 카운트 생성

if (counter == randNumber) {
    servo.write(30);  // 손이 화면을 친다! 
    delay(1000);
    servo.write(180); // 원위치
    
    randNumber = random(50); // 새 랜덤값 생성
}
```

**동작:**
- 랜덤 타이밍 (0~49 카운트)
- 30° → 180° 빠른 회전
- 손이 화면을 치는 효과

---

### 4. 🔊 공포 사운드 (DFPlayer Mini)

**MP3 재생 모듈**

```cpp
DFRobotDFPlayerMini myDFPlayer;

void setup() {
    myDFPlayer.begin(mySoftwareSerial);
    myDFPlayer.volume(30); // 볼륨 설정
    myDFPlayer. play(1);    // 배경음 재생
}
```

**사운드 효과:**
- 배경 공포 브금 루프
- SD 카드에서 재생
- 조절 가능한 볼륨

---

### 5. 💡 LED 조명 (릴레이)

**조명 제어**

```cpp
int relay = 14;

void setup() {
    pinMode(relay, OUTPUT);
    digitalWrite(relay, HIGH); // LED 켜기
}
```

**효과:**
- TV 내부 조명
- 불안한 분위기 연출
- 릴레이로 ON/OFF 제어

---

## 🛠️ 하드웨어 구성

### BOM (Bill of Materials)

| 부품 | 수량 | 용도 | 가격 (KRW) |
|---|---|---|---|
| **Arduino Uno** | 1 | 메인 컨트롤러 | ~15,000 |
| **HC-SR04** | 1 | 초음파 거리 센서 | ~1,500 |
| **KY-040** | 1 | 로터리 엔코더 | ~2,000 |
| **SG90 서보** | 1 | 손 동작 제어 | ~2,500 |
| **DFPlayer Mini** | 1 | MP3 재생 모듈 | ~3,000 |
| **릴레이 모듈** | 1 | LED 제어 | ~1,500 |
| **LED** | 1 | 내부 조명 | ~500 |
| **스피커** | 1 | 사운드 출력 | ~3,000 |
| **Micro SD 카드** | 1 | 음악 파일 저장 | ~5,000 |
| **점퍼 와이어** | 다수 | 연결 | ~3,000 |
| **브레드보드** | 1 | 프로토타입 | ~3,000 |
| **레트로 TV 외관** | 1 | 케이스 | ~10,000 |
| **손 모형** | 1 | 공포 효과 | ~5,000 |
| | | **총합** | **~55,000** |

---

## 🔌 회로 구성

### 핀 연결도

```
Arduino Uno
├── Digital Pin 6  → 서보 모터 (Signal)
├── Digital Pin 7  → 엔코더 CLK
├── Digital Pin 8  → 초음파 Echo
├── Digital Pin 9  → 초음파 Trig
├── Digital Pin 10 → DFPlayer RX
├── Digital Pin 11 → DFPlayer TX
├── Digital Pin 13 → 엔코더 DT
├── Digital Pin 14 (A0) → 릴레이
├── Digital Pin 5  → 엔코더 SW (버튼)
├── 5V  → 센서들 전원
└── GND → 공통 접지
```

### 회로도

```
    Arduino Uno
    ┌─────────────┐
    │             │
    │  D6  ───────┼───→ SG90 서보 (Signal)
    │  D7  ───────┼───→ 엔코더 CLK
    │  D8  ───────┼───→ HC-SR04 Echo
    │  D9  ───────┼───→ HC-SR04 Trig
    │  D10 ───────┼───→ DFPlayer RX
    │  D11 ───────┼───→ DFPlayer TX
    │  D13 ───────┼───→ 엔코더 DT
    │  A0  ───────┼───→ 릴레이 IN
    │  D5  ───────┼───→ 엔코더 SW
    │             │
    │  5V  ───────┼───→ 센서 VCC
    │  GND ───────┼───→ 공통 GND
    └─────────────┘
```

---

## 📦 설치 방법

### 1. Arduino IDE 설치

```bash
# Arduino IDE 다운로드
https://www.arduino.cc/en/software

# 설치 후 실행
```

---

### 2. 라이브러리 설치

**필수 라이브러리:**

1. **DFRobotDFPlayerMini** (MP3 재생)
   ```
   스케치 → 라이브러리 포함하기 → 라이브러리 관리... 
   검색:  "DFRobotDFPlayerMini"
   설치
   ```

2. **Servo** (서보 모터)
   ```
   기본 내장 라이브러리 (별도 설치 불필요)
   ```

3. **SoftwareSerial** (시리얼 통신)
   ```
   기본 내장 라이브러리
   ```

---

### 3. 코드 업로드

```bash
# 1. 저장소 클론
git clone https://github.com/Deamonio/Horror_TV.git
cd Horror_TV

# 2. Arduino IDE에서 main. ino 열기
파일 → 열기 → main.ino

# 3. 보드 설정
도구 → 보드 → Arduino Uno

# 4. 포트 선택
도구 → 포트 → COM3 (Windows) / /dev/ttyUSB0 (Linux)

# 5. 업로드
스케치 → 업로드 (Ctrl+U)
```

---

### 4. SD 카드 준비

**음악 파일 준비:**

```bash
# SD 카드 포맷 (FAT32)
# 음악 파일 복사
001.mp3  # 공포 배경음

# SD 카드를 DFPlayer Mini에 삽입
```

**파일 명명 규칙:**
- `001.mp3`, `002.mp3`...   형식
- FAT32 포맷
- 최대 32GB

---

## 🚀 사용 방법

### 1️⃣ 하드웨어 조립

**순서:**

1. **회로 연결**
   - 위의 핀 연결도대로 배선
   - 브레드보드 사용 권장

2. **서보 모터 장착**
   - TV 내부에 서보 고정
   - 손 모형을 서보 암에 연결

3. **엔코더 다이얼**
   - TV 외부에 엔코더 노브 설치
   - 채널 다이얼처럼 보이게 배치

4. **초음파 센서**
   - TV 전면에 센서 배치
   - 사용자 감지 범위:  10cm

5. **스피커 & LED**
   - 스피커 내부 배치
   - LED 은은하게 설치

---

### 2️⃣ 전원 연결

```
Arduino Uno → USB 케이블 → 5V 전원 어댑터
```

**전원 투입 시:**
- LED 켜짐
- 공포 배경음 재생 시작
- 서보 모터 초기 위치 (180°)
- 시리얼 모니터에 "DFPlayer Mini online" 출력

---

### 3️⃣ 체험하기

**시나리오:**

1. **접근**
   ```
   사용자가 TV 앞 10cm 이내 접근
   → 초음파 센서 감지
   → 시스템 활성화
   ```

2. **다이얼 회전**
   ```
   로터리 엔코더 다이얼을 시계방향/반시계방향 회전
   → 카운터 증가/감소
   → 시리얼 모니터에 "rotate:  right | Counter: 5" 출력
   ```

3. **서프라이즈!   **
   ```
   카운터가 랜덤 숫자와 일치하면... 
   → 서보 모터 작동 (30°)
   → 손이 화면을 친다!  💥
   → 1초 후 원위치 (180°)
   → 새 랜덤값 생성
   ```

---

### 4️⃣ 시리얼 모니터 디버깅

**Arduino IDE에서:**
```
도구 → 시리얼 모니터 (Ctrl+Shift+M)
```

**출력 예시:**
```
DFPlayer Mini online. 
random:   12
rotate:  right | Counter: 1
rotate: right | Counter: 2
... 
rotate: right | Counter: 12
💥 [서보 작동!]
random:  27
```

---

## 💻 코드 분석

### 핵심 로직

#### 1. Setup (초기화)

```cpp
void setup() {
    // 1. 시리얼 통신 시작
    Serial.begin(115200);
    mySoftwareSerial.begin(9600);
    
    // 2. 하드웨어 핀 설정
    pinMode(trig, OUTPUT);
    pinMode(echo, INPUT);
    pinMode(relay, OUTPUT);
    servo.attach(servoPin);
    
    // 3. DFPlayer 초기화
    myDFPlayer.begin(mySoftwareSerial);
    myDFPlayer.volume(30);
    myDFPlayer.play(1); // 배경음 재생
    
    // 4. LED 켜기
    digitalWrite(relay, HIGH);
    
    // 5. 서보 초기 위치
    servo.write(180);
    
    // 6. 랜덤 시드 생성
    randomSeed(analogRead(0));
    randNumber = random(3);
}
```

---

#### 2. Loop (메인 루프)

```cpp
void loop() {
    // 1. 초음파 거리 측정
    long duration, distance;
    digitalWrite(trig, LOW);
    delayMicroseconds(2);
    digitalWrite(trig, HIGH);
    delayMicroseconds(10);
    digitalWrite(trig, LOW);
    duration = pulseIn(echo, HIGH);
    distance = duration * 17 / 1000;
    
    // 2. 10cm 이내 접근 시만 작동
    if (distance <= 10) {
        // 3. 엔코더 회전 감지
        currentStateCLK = digitalRead(CLK);
        
        if (currentStateCLK != lastStateCLK && currentStateCLK == 1) {
            // 4. 회전 방향 판단
            if (digitalRead(DT) != currentStateCLK) {
                counter++;   // 시계방향
            } else {
                counter--;   // 반시계방향
            }
        }
        lastStateCLK = currentStateCLK;
        
        // 5. 랜덤 타이밍 도달 시 서프라이즈! 
        if (counter == randNumber) {
            servo.write(30);   // 손이 화면을 친다!
            delay(1000);
            servo.write(180);  // 원위치
            randNumber = random(50); // 새 랜덤값
        }
    }
}
```

---

### 알고리즘 흐름도

```
시작
  ↓
초기화 (setup)
  ↓
무한 루프 (loop)
  ├─→ 거리 측정
  ├─→ 10cm 초과?  → 대기
  └─→ 10cm 이하? 
       ├─→ 엔코더 회전 감지
       ├─→ 카운터 업데이트
       └─→ 카운터 == 랜덤? 
            ├─→ NO → 계속 대기
            └─→ YES
                 ├─→ 서보 작동 (30°)
                 ├─→ 1초 대기
                 ├─→ 서보 복귀 (180°)
                 └─→ 새 랜덤값 생성
```

---

## 🎨 커스터마이징

### 1. 서프라이즈 타이밍 조정

```cpp
// 더 빠른 서프라이즈
randNumber = random(10);  // 0~9 (기존:  0~49)

// 더 느린 서프라이즈
randNumber = random(100); // 0~99
```

---

### 2. 감지 거리 변경

```cpp
// 더 멀리서 감지
if (distance <= 20) { // 20cm (기존: 10cm)

// 더 가까이서만 감지
if (distance <= 5) {  // 5cm
```

---

### 3. 서보 속도 조정

```cpp
// 더 빠른 동작
servo.write(30);
delay(500);  // 0.5초 (기존: 1초)
servo.write(180);

// 더 느린 동작
servo.write(30);
delay(2000); // 2초
servo.write(180);
```

---

### 4. 볼륨 조정

```cpp
// 더 크게
myDFPlayer.volume(30); // 최대 30 (기존값)

// 더 작게
myDFPlayer.volume(15); // 절반
```

---

### 5. 다중 사운드 추가

```cpp
// 랜덤 사운드 재생
int soundNum = random(1, 4); // 001.mp3 ~ 003.mp3
myDFPlayer.play(soundNum);
```

---

## 🔧 문제 해결

### 1. DFPlayer 초기화 실패

**증상:**
```
Unable to begin: 
1. Please recheck the connection! 
2.Please insert the SD card!
```

**해결:**
```bash
# 1. SD 카드 확인
- FAT32 포맷
- 용량 32GB 이하
- 음악 파일 001.mp3 형식

# 2. 배선 확인
RX(D10) → DFPlayer TX
TX(D11) → DFPlayer RX

# 3. 전원 확인
DFPlayer VCC → 5V
DFPlayer GND → GND
```

---

### 2. 서보 모터 떨림

**증상:**
- 서보가 미세하게 떨림
- 위치 불안정

**해결:**
```cpp
// 1. 전원 분리
// 서보에 외부 5V 전원 사용 (Arduino와 GND만 공유)

// 2. 캐패시터 추가
// 서보 전원선에 100μF 캐패시터

// 3. 코드 안정화
servo.  write(180);
delay(100); // 안정화 시간
```

---

### 3. 엔코더 오작동

**증상:**
- 카운터가 제멋대로 증가/감소
- 회전 방향 잘못 감지

**해결:**
```cpp
// 1. 디바운싱 추가
unsigned long lastDebounceTime = 0;
unsigned long debounceDelay = 50;

if ((millis() - lastDebounceTime) > debounceDelay) {
    // 엔코더 읽기
    lastDebounceTime = millis();
}

// 2. 풀업 저항 확인
pinMode(CLK, INPUT_PULLUP);
pinMode(DT, INPUT_PULLUP);
```

---

### 4. 초음파 센서 오차

**증상:**
- 거리 측정 부정확
- 감지 안 됨

**해결:**
```cpp
// 1. 평균값 사용
long distances[5];
for (int i = 0; i < 5; i++) {
    // 거리 측정
    distances[i] = distance;
    delay(10);
}
long avgDistance = (distances[0] + ...  + distances[4]) / 5;

// 2. 센서 각도 조정
// 초음파 센서는 정면 15° 이내에서 정확
```

---

## 📊 성능 지표

### 시스템 성능

| 항목 | 성능 |
|---|---|
| **반응 속도** | ~50ms (초음파 측정) |
| **서보 동작 시간** | 1초 |
| **감지 거리** | 10cm (조정 가능) |
| **랜덤 범위** | 0~49 (기본값) |
| **전력 소비** | ~1.5W |

---

## 🎯 활용 사례

### 1. 할로윈 장식

```
집 앞 현관에 설치
→ 트릭 오어 트리트 어린이 체험
→ 무해하지만 놀라운 효과
```

### 2. 방탈출 카페

```
공포 테마 방
→ 힌트 찾기 중 서프라이즈
→ 몰입감 극대화
```

### 3. 전시회/박물관

```
인터랙티브 아트 설치
→ 관람객 참여 유도
→ SNS 바이럴 효과
```

---

## 🚀 향후 개선 사항

- [ ] 다중 서프라이즈 패턴 (손 + 얼굴)
- [ ] 음성 인식 (특정 말에 반응)
- [ ] Wi-Fi 연동 (원격 제어)
- [ ] 영상 재생 (실제 TV 화면처럼)
- [ ] 연기 효과 추가

---

## 🤝 기여하기

기여는 언제나 환영합니다!    🎉

### 기여 방법

1. Fork 이 저장소
2. Feature 브랜치 생성:  `git checkout -b feature/AmazingFeature`
3. 변경사항 커밋: `git commit -m 'Add some AmazingFeature'`
4. 브랜치에 Push: `git push origin feature/AmazingFeature`
5. Pull Request 생성

---

## 📜 라이선스

이 프로젝트는 MIT License 하에 배포됩니다.

---

## 📞 연락처

<div align="center">

### 프로젝트 관리자:   Deamonio

[![Email](https://img.shields.io/badge/Email-hyun0810d@gmail.com-EA4335?style=for-the-badge&logo=gmail&logoColor=white)](mailto:hyun0810d@gmail.com)
[![GitHub](https://img.shields.io/badge/GitHub-Deamonio-181717?style=for-the-badge&logo=github&logoColor=white)](https://github.com/Deamonio)

**프로젝트 링크**:  [https://github.com/Deamonio/Horror_TV](https://github.com/Deamonio/Horror_TV)

</div>

---

## 🙏 감사의 말

| Arduino | DFRobot | Servo | HC-SR04 |
|---|---|---|---|
| 플랫폼 | MP3 모듈 | 모터 제어 | 거리 측정 |

**특별 감사:**
- 🎃 **Arduino Community** - 풍부한 예제와 라이브러리
- 🔊 **DFRobot** - 훌륭한 MP3 재생 모듈
- 👻 **Horror Fans** - 프로젝트 영감

---

<div align="center">

## ⭐ 이 프로젝트가 마음에 드셨다면 Star를 눌러주세요!  

[![Star History Chart](https://api.star-history.com/svg?repos=Deamonio/Horror_TV&type=Date)](https://star-history.com/#Deamonio/Horror_TV&Date)

---

**Made with 👻 by Deamonio**

*"Turning simple electronics into spine-chilling experiences"*

---

**© 2025 Deamonio.   All rights reserved.**

[⬆ 맨 위로 돌아가기](#-horror-tv)

</div>
